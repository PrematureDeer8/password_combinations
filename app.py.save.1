import time
import string
import multiprocessing as mp
import numpy as np;
from math import log;

digits  = int(input("How many characters is the password: "));

length  = len(string.ascii_lowercase);

lets = [];
combos = [];

counter = 0;

for i in range(0, length**digits):
	subtract = int(log((i+1), length));
	if((i+1)%length == 0):
		counter +=1
		if(counter == 2):
			break;
		combos.append(lets);
		lets = [];
	combination = "a"*digits;
	iterator = int((i+1) - (length * counter));
	for dig in range(0,digits):
		if((i+1)%(length**(dig+1)) == 0):
			current_one  = string.ascii_lowercase.
			combination[digits-dig] = string.ascii_lowercase[
		combination[-1] = string.ascii_lowercase[iterator]
	#print(combination);
	lets.append(combination);

#print(len(lets));
